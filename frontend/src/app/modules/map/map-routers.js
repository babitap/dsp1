import Map from './views/map'

const mapRoutes = [
    {
        path: 'map',
        name: 'map',
        component: Map,
        meta: {
        breadcrumb: [
            { name: 'MAP' }
        ]
        }
    }
]

export default mapRoutes
